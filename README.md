# Phishing_Detection_AI_Agent
멀티모달 분석 결과 종합 및 피싱 위험도 판단 Agent

1. 환경변수 설정
.env.example 참고해 .env 파일 설정

2. 패키지 설치
pip install -r requirements.txt

3. 실행
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

4. 접속
http://localhost:8000/docs
