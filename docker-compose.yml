version: "3.8"
services:
  agent:
    build:
      context: .
    image: phishing-agent:latest
    container_name: agent-container
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}